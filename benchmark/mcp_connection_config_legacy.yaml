tasks:
  # Legacy config — uses direct server paths instead of /app/mcp_dispatch.py.
  # Works with any image version (old or new) since the original scripts are unchanged.
  #
  # Use this if you haven't rebuilt the image yet:
  #   python benchmark_runner.py --mcp-config benchmark/mcp_connection_config_legacy.yaml ...
  #
  # The default mcp_connection_config.yaml uses the dispatcher (/app/mcp_dispatch.py)
  # and requires the image to be rebuilt after the dispatcher was added:
  #   make build && docker compose up -d

  # Capability 1 (bi_apis): ToolCallingAgent + RouterMCPServer
  1:
    mode: stdio
    container_name: capability_1_bi_apis
    container_runtime: null
    container_env:
      MCP_DB_ROOT: /app/db
      MCP_SERVER_TYPE: router
    container_command:
      - python
      - "-m"
      - "environment.m3.python_tools.mcp"

  # Capability 2 (dashboard_apis): LangGraphReActAgent + FastAPIMCPServer
  2:
    mode: stdio
    container_name: capability_2_dashboard_apis
    container_runtime: null
    container_env:
      MCP_DB_ROOT: /app/db
    container_command:
      - python
      - /app/m3-rest/mcp_server.py

  # Capability 3 (multihop_reasoning): LangGraphReActAgent + bpo_router (routes by MCP_DOMAIN)
  # MCP_DOMAIN=bpo     → /app/environment/bpo/mcp/server.py  (BPO FastMCP server)
  # MCP_DOMAIN=<other> → /app/m3-rest/mcp_server.py   (M3 REST MCP server)
  3:
    mode: stdio
    container_name: capability_3_multihop_reasoning
    container_runtime: null
    container_env:
      MCP_DB_ROOT: /app/db
    container_command:
      - python
      - /app/environment/bpo/mcp/bpo_router.py

  # Capability 4 (multiturn): LangGraphReActAgent + Capability4CombinedMCPServer
  4:
    mode: stdio
    container_name: capability_4_multiturn
    container_runtime: null
    container_env:
      MCP_DB_ROOT: /app/db
      PRELOAD_COLLECTIONS: "false"
    container_command:
      - python
      - /app/retrievers/capability_4_mcp_server.py
